# ./docker/nginx/default.conf
server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /nginx_status {
        stub_status on;
        allow 127.0.0.1;
        deny all;
    }
}